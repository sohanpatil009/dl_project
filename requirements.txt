# Core scientific stack (Colab-compatible: no upper pin on numpy;
# Colab ships numpy 2.x + torch preinstalled — a `<2.0` pin forces a
# downgrade and breaks torch/ultralytics/opencv at import time)
numpy>=1.23
scipy>=1.10
matplotlib>=3.6
pandas>=1.5
scikit-learn>=1.2
tqdm>=4.64
pyyaml>=6.0

# Signal processing
# (scipy.signal is enough; no extra deps)

# PyTorch (CPU build is fine, CUDA is auto-detected at runtime)
torch>=2.0,<3.0
torchvision>=0.15

# YOLOv5 dependencies (Ultralytics repo will be cloned at runtime)
# Pinned to be compatible with Ultralytics YOLOv5 v7.0+
# NOTE on Colab/Kaggle: torch/torchvision/jupyter are preinstalled with CUDA.
# The Colab notebook (§4) filters those lines out automatically so pip
# does not reinstall a ~2 GB torch build or downgrade numpy.
Pillow>=9.0
PyYAML>=5.4.1
tqdm>=4.64.0
ultralytics>=8.0.0
opencv-python-headless>=4.6

# Notebook / dashboard utilities
ipython>=8.0
ipykernel>=6.0
jupyter>=1.0
notebook>=6.5

# Testing
pytest>=7.2
pytest-cov>=4.0

# Optional: pretty printing
rich>=12.0
